name: Linters

on:
  pull_request: {}

permissions:
  contents: write

jobs:
  linters:
    name: linters
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: pip

      - name: Install pre-commit
        run: pip install pre-commit==3.8.0

      - name: Run pre-commit on the pull-request diff
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -o pipefail
          pre-commit run --from-ref "$BASE_SHA" --to-ref "$HEAD_SHA" --show-diff-on-failure 2>&1 | tee precommit.log

      - name: Package automatic pre-commit fixes
        if: always()
        run: |
          git diff --name-only --diff-filter=ACMRT > precommit-modified-files.txt
          if [ -s precommit-modified-files.txt ]; then
            tar -czf precommit-fixes.tar.gz -T precommit-modified-files.txt
          else
            tar -czf precommit-fixes.tar.gz --files-from /dev/null
          fi

      - name: Upload pre-commit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: precommit-${{ github.event.pull_request.head.sha }}
          path: |
            precommit.log
            precommit-modified-files.txt
            precommit-fixes.tar.gz
          if-no-files-found: warn
          retention-days: 30

  semgrep:
    name: semgrep
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: pip

      - name: Download Semgrep rules
        run: git clone --depth 1 https://github.com/frappe/semgrep-rules.git frappe-semgrep-rules

      - name: Download Semgrep
        run: pip install semgrep

      - name: Run Semgrep rules
        run: |
          set -o pipefail
          semgrep ci --config ./frappe-semgrep-rules/rules --config r/python.lang.correctness 2>&1 | tee semgrep.log

      - name: Upload Semgrep evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-${{ github.event.pull_request.head.sha }}
          path: semgrep.log
          if-no-files-found: warn
          retention-days: 30

  apply-corrections:
    if: github.event.pull_request.head.ref == 'reconstruccion-definitiva-construcontrol'
    name: Apply canonical correction batch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: reconstruccion-definitiva-construcontrol
          fetch-depth: 0

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install exact pre-commit runner
        run: pip install pre-commit==3.8.0

      - name: Apply lint, Semgrep and runtime corrections
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          git pull --ff-only origin reconstruccion-definitiva-construcontrol
          pre-commit run --from-ref "$BASE_SHA" --to-ref "$HEAD_SHA" --show-diff-on-failure || true

          python - <<'PY'
          from pathlib import Path

          replacements = {
              "erpnext/construcontrol/construction.py": [
                  ("isinstance(value, (int, float))", "isinstance(value, int | float)"),
              ],
              "erpnext/construcontrol/tests/test_expense_status_schema_standalone.py": [
                  (
                      "isinstance(node, (ast.Assign, ast.AnnAssign))",
                      "isinstance(node, ast.Assign | ast.AnnAssign)",
                  ),
              ],
              "erpnext/construcontrol/tests/test_fi02_lifecycle_standalone.py": [
                  (
                      "isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef))",
                      "isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)",
                  ),
              ],
              "scripts/validate_construcontrol_product.py": [
                  (
                      "isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef))",
                      "isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)",
                  ),
              ],
              "erpnext/construcontrol/page/construcontrol_reporting_center/construcontrol_reporting_center.js": [
                  (
                      'frappe.msgprint("Seleccione un proyecto para generar, exportar o notificar.");',
                      'frappe.msgprint(__("Seleccione un proyecto para generar, exportar o notificar."));',
                  ),
              ],
              "erpnext/construcontrol/tests/test_install_entrypoint_standalone.py": [
                  (
                      'frappe.flags = flags if flags is not None else Flags()',
                      'setattr(frappe, "flags", flags if flags is not None else Flags())',
                  ),
              ],
          }

          for relative, pairs in replacements.items():
              path = Path(relative)
              text = path.read_text(encoding="utf-8")
              for old, new in pairs:
                  count = text.count(old)
                  if count != 1:
                      raise RuntimeError(
                          f"{relative}: expected one occurrence of {old!r}, found {count}"
                      )
                  text = text.replace(old, new)
              path.write_text(text, encoding="utf-8")

          hooks = Path("erpnext/hooks.py")
          text = hooks.read_text(encoding="utf-8")
          text = text.replace("  # noqa: F401,F403", "")
          text = text.replace("  # noqa: F405", "")
          hooks.write_text(text, encoding="utf-8")

          weekly = Path("erpnext/construcontrol/weekly_impl.py")
          text = weekly.read_text(encoding="utf-8")
          helper_marker = "\ndef _snapshot(project: str, start: Any, end: Any) -> dict[str, Any]:\n"
          helper = (
              "\n\n"
              "def _quality_failure_count(project: str, start: Any, end: Any) -> int:\n"
              "\tmeta = frappe.get_meta(\"CC Progress Update\")\n"
              "\tquality_field = next(\n"
              "\t\t(fieldname for fieldname in (\"quality_status\", \"quality\") if meta.has_field(fieldname)),\n"
              "\t\t\"\",\n"
              "\t)\n"
              "\tif not quality_field:\n"
              "\t\treturn 0\n"
              "\tfilters: dict[str, Any] = {\n"
              "\t\t**_filters(project),\n"
              "\t\t\"is_logically_deleted\": 0,\n"
              "\t\t\"posting_date\": [\"between\", [start, end]],\n"
              "\t\tquality_field: [\n"
              "\t\t\t\"in\",\n"
              "\t\t\t[\"failed\", \"rejected\", \"non_compliant\", \"attention\", \"poor\", \"corrective\"],\n"
              "\t\t],\n"
              "\t}\n"
              "\tif meta.has_field(\"incident_status\"):\n"
              "\t\tfilters[\"incident_status\"] = [\"!=\", \"resolved\"]\n"
              "\treturn frappe.db.count(\"CC Progress Update\", filters)\n"
          )
          if helper_marker not in text:
              raise RuntimeError("weekly_impl.py: snapshot marker not found")
          text = text.replace(helper_marker, helper + helper_marker, 1)
          start_marker = "\tquality_failures = frappe.db.count(\n"
          end_marker = "\n\ttry:\n"
          start_index = text.find(start_marker)
          end_index = text.find(end_marker, start_index)
          if start_index < 0 or end_index < 0:
              raise RuntimeError("weekly_impl.py: legacy quality query not found")
          text = (
              text[:start_index]
              + "\tquality_failures = _quality_failure_count(project, start, end)\n"
              + text[end_index:]
          )
          weekly.write_text(text, encoding="utf-8")
          PY

          rm -f .github/workflows/apply-lint-fixes-once.yml
          git show 0ea477e9cba8df813872ba92eaa5041950eb414c:.github/workflows/linters.yml > .github/workflows/linters.yml
          pre-commit run --from-ref "$BASE_SHA" --to-ref "$HEAD_SHA" --show-diff-on-failure || true
          pre-commit run --from-ref "$BASE_SHA" --to-ref "$HEAD_SHA" --show-diff-on-failure
          git diff --check

          git config user.name "ConstruControl CI"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "fix(runtime): apply lint and weekly schema corrections"
          git push origin HEAD:reconstruccion-definitiva-construcontrol
