# NEXORA — Matriz canónica de requisitos

## Criterio de autoridad

Esta matriz raíz existe porque no se encontró una versión histórica de `MATRIZ_REQUISITOS.md` en la raíz del repositorio. La matriz amplia existente en `docs/nexora/MATRIZ_REQUISITOS.md` se conserva como antecedente operativo, pero esta versión canónica aplica la taxonomía solicitada y queda subordinada a `NEXORA_CONSTITUTION.md`, `EXECUTION_STATE.md`, `docs/nexora/` y las pruebas bajo `nexora_app/nexora/tests/`.

Ningún requisito se marca como `IMPLEMENTADO Y VALIDADO` en este documento: aunque hay evidencia de código y pruebas para varios dominios, el criterio pedido exige además commit/SHA/CI de este lote y validación integral acumulada. Hasta completar ese ciclo, la clasificación se limita a estados de existencia, confirmación o activación externa pendiente.

## Estados permitidos

`CONFIRMADO`, `PROPUESTO`, `REQUIERE DECISIÓN`, `EXISTENTE Y REUTILIZABLE`, `EXISTENTE PERO DEFECTUOSO`, `OBSOLETO`, `OBSOLETO JUSTIFICADO`, `NO DEMOSTRADO`, `IMPLEMENTADO Y VALIDADO`, `IMPLEMENTADO — ACTIVACIÓN EXTERNA PENDIENTE`.

## Matriz resumida

| ID | Requisito | Estado | Evidencia canónica | Criterio para elevar estado |
|---|---|---|---|---|
| `NXR-GOV-001` | NEXORA es el producto visible único; ERPNext/Frappe operan como motor interno. | CONFIRMADO | `NEXORA_CONSTITUTION.md`, `AGENTS.md`, `docs/nexora/ARQUITECTURA.md`. | Mantener navegación, textos y rutas sin identidad paralela ConstruControl/ERPNext visible al usuario ordinario. |
| `NXR-GOV-002` | La fuente de verdad final es `main` del repositorio `Clopezgg/Gesti-n-de-Construcci-n-Residencial`. | CONFIRMADO | Sesión Bloque 46: `git remote -v` apunta a `github.com/Clopezgg/Gesti-n-de-Construcci-n-Residencial`; `git status` reporta `up to date with origin/main`; `git ls-remote origin main` coincide con el HEAD local `2b238f0dd7462f3aa0ff7bb703b69a1488a5b613`. | Mantener sincronía en cada lote futuro: confirmar SHA remoto tras cada push. |
| `NXR-GOV-003` | La documentación canónica usa una sola taxonomía de estados permitidos. | CONFIRMADO | Este archivo, `PLAN_MAESTRO.md` y `NEXORA_GOLDEN_PATHS.md` declaran la taxonomía permitida. | Validación automática que rechace estados fuera de catálogo en documentos raíz y `docs/nexora/`. |
| `NXR-UX-001` | Inicio, navegación principal, dashboard, reportes y páginas críticas deben estar disponibles desde NEXORA. | EXISTENTE Y REUTILIZABLE | Tests de contrato de páginas, shell, dashboard, PWA y registry en `nexora_app/nexora/tests/`. Auditoría acotada posterior al Bloque 59: página nueva `nexora-notifications` conecta `notifications.service` (crear/reintentar/listar/marcar leída, `NXR-NOT-0006`) — antes ni el propio destinatario podía ver o marcar como leída una notificación suya sin llamar la API a mano. | Prueba browser real escritorio/iPhone/PWA en CI del SHA publicado. |
| `NXR-FIN-001` | Fondos, fuentes, operaciones, efectos, compromisos, saldos e idempotencia comparten un ledger financiero único. | EXISTENTE Y REUTILIZABLE | `docs/nexora/MATRIZ_REQUISITOS.md`, tests `test_financial_core.py`, `test_ledger_core.py`, `test_remittance_contract.py`. | Evidencia integrada de permisos server-side, auditoría, errores, rollback y CI verde del SHA publicado. |
| `NXR-FIN-002` | Un gasto paga desde una sola fuente; las transferencias internas son el mecanismo para mover fondos. | CONFIRMADO | `EXECUTION_STATE.md` registra la reversión del gasto multi-fuente; `docs/nexora/MATRIZ_REQUISITOS.md` conserva `NXR-FND-0005` como `OBSOLETO JUSTIFICADO`. | Prueba negativa de gasto multi-fuente y prueba positiva de transferencia interna en CI. |
| `NXR-FIN-003` | La remesa multi-destino conserva trazabilidad, redondeo correcto y fuentes independientes. | EXISTENTE Y REUTILIZABLE | Entradas de bloques 38-39 en `EXECUTION_STATE.md`; tests de remesas y financiero. | Ejecución integrada Frappe/MariaDB más CI verde del SHA publicado. |
| `NXR-OPS-001` | Las operaciones diarias piden datos conocidos por el usuario y derivan lo técnico en backend. | EXISTENTE Y REUTILIZABLE | Tests de consola operativa, wizard guiado, correcciones y modelo financiero. | Validación visual y negativa de errores comprensibles en browser real. |
| `NXR-CIE-001` | Cierre mensual → fotografía real calculada → aprobación → corrección enlazada, sin sobrescribir el original. | EXISTENTE Y REUTILIZABLE | `close.monthly_canonical` (completo: `_calculate` sobre `get_executive_snapshot`, `create/transition/correct/list_monthly_close`), enrutado vía `hooks.py::override_whitelisted_methods` desde `close.service.*`; `test_monthly_close_contract.py` (3/5 verdes localmente, 2 requieren Frappe). Bloque 52: `nexora_closing.js` extendida con la sección "Cierre mensual" — antes el backend completo no tenía ninguna interfaz. | Ejecución real en CI (crear, aprobar, corregir un cierre) y navegación real en navegador. |
| `NXR-PRE-001` | Presupuesto: crear, activar, enmendar (versión enlazada), cerrar/cancelar, sin sobregiro (`validate_no_overspend`) y conectado a compromisos reales (`reserve_budget_commitment`). | EXISTENTE Y REUTILIZABLE | `budget/service.py` (completo salvo lectura) + `budget/core.py`; `test_budget_contract.py`/`test_budget_core.py`. Bloque 53: se agregaron `get_budget`/`list_budgets` (no existía ninguna lectura, a diferencia de compras/inventario/cierre) y la página `nexora-budget` — antes solo era posible operar presupuestos llamando la API a mano. | Ejecución real en CI (crear, activar, enmendar, verificar sobregiro) y navegación real en navegador. |
| `NXR-CAL-001` | Control de calidad: crear, transicionar (`Open→Passed/Failed→Corrected→...→Closed`), listar por proyecto/estado, con resultado y acciones correctivas. | EXISTENTE Y REUTILIZABLE | `quality/service.py` (completo: create/transition/list, existente desde el Bloque 13) + `quality/core.py`; `test_quality_contract.py` (nueva, 5/5 verdes localmente), `test_quality_core.py`. Bloque 54: página `nexora-quality` — el servicio nunca tuvo ningún punto de entrada real, ni un Administrador podía crear o transicionar un control fuera del propio doctype. | Ejecución real en CI y navegación real en navegador. |
| `NXR-SEC-001` | Todo flujo de escritura usa permisos server-side, auditoría e idempotencia cuando aplique. | EXISTENTE Y REUTILIZABLE | Tests `test_service_locked_permission_integration.py`, `test_security_project_scoping_contract.py`, `test_integrations_audit_contract.py`. | Revisión de cobertura por DocType/servicio y CI verde del SHA publicado. |
| `NXR-PUR-001` | Compras: solicitud, cotización, orden, recepción y puente financiero deben integrarse sin duplicar saldos. | EXISTENTE Y REUTILIZABLE | Tests de purchase request/order/quotation/receipt y puente financiero. Bloque 50: página `nexora-purchase-orders` reemplaza el escritorio técnico de Frappe para el paso "orden". Bloque 57: página `nexora-receipts` para el paso "recepción" — antes solo alcanzable llamando la API a mano. GP-04 completo con página NEXORA en cada paso (solicitud → cotización → orden → recepción; el pago vive en la propia página de órdenes desde el Bloque 50). *Corrección (Bloque 96):* "pendiente por falta de docker/bench" ya no aplica — `test_purchase_integration`/`test_purchase_request_integration`/`test_quotation_integration`/`test_receipt_integration`/`test_purchase_payment_integration` corren todos en el job `mariadb` de `nexora-financial.yml`; las cuatro páginas del recorrido (`nexora-purchase-requests`/`nexora-quotations`/`nexora-purchase-orders`/`nexora-receipts`) se navegan con aserciones reales de DOM (`waitFor visible` + `networkidle`, no un no-op) en `validateModuleGallery` (`scripts/nexora_browser_validators.mjs`), dentro del job `Frappe real · escritorio · tableta · iPhone · PWA`; y GP-04 ya cerró sus tres pruebas negativas (Bloque 80/82). | Recorrido, permisos, pruebas negativas y CI real en navegador ya tienen evidencia verde y confirmada. |
| `NXR-INV-001` | Inventario usa un ledger canónico y prohíbe saldo físico negativo. | EXISTENTE Y REUTILIZABLE | Tests `test_inventory_core.py`, `test_inventory_integration.py`; historial reciente menciona corrección de inventario negativo. Bloque 51: página `nexora-inventory` (nueva sección "Inventario" en la navegación) — antes `inventory.service` no tenía ninguna interfaz, solo el panel de inventario crítico del dashboard (lectura posterior al hecho). | Prueba integrada de recepción/salida/rollback en Frappe/MariaDB; navegación real de la página nueva. |
| `NXR-CON-001` | Contratos, adendas, estimaciones, anticipos, pagos y auditoría se relacionan con entidades reales. | EXISTENTE Y REUTILIZABLE | Tests de contratos, cotizaciones, evidencias y directorio. | Golden path visual contrato → estimación → pago con CI y permisos. |
| `NXR-EVI-001` | Evidencia documental/fotográfica queda vinculada a proyecto, operación, contrato o avance. | EXISTENTE Y REUTILIZABLE | Tests `test_evidence_core.py`, `test_evidence_contract.py`, Context360/timeline. | Prueba visual de captura y consulta histórica en escritorio/iPhone/PWA. |
| `NXR-REP-001` | Reportes y exportaciones respetan alcance de proyecto, permisos y errores explícitos. | EXISTENTE Y REUTILIZABLE | Tests de reportes, vistas canónicas, export guard y dashboard por proyecto. | Validación integrada con usuarios de distinto rol y CI. |
| `NXR-PWA-001` | NEXORA debe ser responsive, instalable como PWA y usable en iPhone/escritorio. | EXISTENTE Y REUTILIZABLE | Playwright real (no solo contrato) en CI: `validatePwa()` corre en `desktop-chromium`/`ipad-gen7-webkit`/`iphone-13-webkit` — manifest válido, service worker activo, caché de shell sin recursos sensibles, banner offline y recuperación al reconectar. Verde bajo `pipefail` contra `main` (`d654740`, PR #232), 92 artefactos (incluye capturas por perfil) subidos. Ver `GP-12`. | Fallo de caché corrupta/expirada y revisión visual manual de los artefactos ya subidos (no solo el assert automatizado). |
| `NXR-AI-001` | Consulta natural y asistentes usan datos reales, permisos existentes y no crean un segundo backend operativo. | EXISTENTE Y REUTILIZABLE | Tests de conversación, inteligencia, gateway, router y exposición de datos. Auditoría acotada post-Bloque 57: `nexora_ai_providers.js` conectó `check_provider_readiness`/`get_provider_runtime_config`/`list_active_providers`/`get_provider_capabilities`/`preview_routing_decision` — cinco funciones reales del panel de administración que no tenían ningún llamador. `resolve_capability` (Bloque 1) se deja sin conectar a propósito: superseded por `preview_routing_decision`, conectar ambas daría dos respuestas distintas a la misma pregunta. | Proveedor real o stub controlado en CI, auditoría y confirmación de acciones. |
| `NXR-INT-001` | Integraciones externas (SAP, WhatsApp, proveedores IA) quedan listas sin fabricar éxito operacional. | IMPLEMENTADO — ACTIVACIÓN EXTERNA PENDIENTE | Tests de conectividad/contrato y documentación NIP; requieren credenciales reales externas. Auditoría acotada posterior al Bloque 58: página nueva `nexora-integrations` conecta `integrations.service` (registro genérico REST/SOAP/Webhook, prueba de conectividad) e `integrations.sap` (conectar/probar conexión SAP, enviar documento) — siete funciones reales sin ningún punto de entrada hasta ahora. WhatsApp (`nexora-conversation-channels`) y proveedores de IA (`nexora-ai-providers`) ya tenían página propia. | Credenciales reales, ejecución contra servicio externo, auditoría y CI del SHA publicado. |
| `NXR-ADM-001` | NEXORA tiene una zona funcional propia para administrar usuarios, roles, activación y desactivación, separada de la cuenta técnica `Administrator` (Constitución Cap. 14, enmienda del propietario 2026-08-16). | EXISTENTE Y REUTILIZABLE | `nexora.administration.service`/`core`, página `nexora-administracion` enlazada en `nexora_shell.js` y en el workspace legado; `test_administration_core.py` (15/15 verdes localmente) y `test_administration_contract.py` (9/9 verdes localmente). *Corrección (Bloque 92):* "pendiente de bench/MariaDB real" ya no aplica — `test_administration_integration.py` corre en el job `mariadb` de `nexora-financial.yml` desde el PR #231 (huérfano encontrado y conectado ese mismo bloque); confirmado en verde en una corrida reciente (PR #249, 19 tests, "OK", posición 7 de la segunda tanda de módulos). La navegación real de la página `nexora-administracion` (`#page-nexora-administracion .nxr-admin`) también corre ya en el job `Frappe real · escritorio · tableta · iPhone · PWA` desde el Bloque 66 (`scripts/nexora_browser_validators.mjs`). *Corrección (Bloque 93):* la cobertura de permisos server-side/auditoría/errores que quedó sin evaluar en el Bloque 92 se confirmó leyendo las 19 pruebas reales de `test_administration_integration.py`: permisos negativos por rol en las cuatro acciones (`NEXORA Finance Manager`, `NEXORA Auditor`, usuario sin rol NEXORA), exclusión real de `Administrator`/`Guest` en el listado, protección de la propia sesión, protección del último Administrador NEXORA activo (por estado y por rol, dos rutas distintas), pureza del conjunto de roles NEXORA (rechaza roles técnicos, nunca toca roles preexistentes fuera del conjunto) y auditoría real (`NXR Audit Event`) tras cada mutación exitosa — no solo el conteo agregado "19 tests, OK" que bastaba para el Bloque 92. | Cobertura de permisos/auditoría/errores confirmada. Para elevar a `IMPLEMENTADO Y VALIDADO` falta el ciclo de validación acumulada de todo el documento que exige la regla del encabezado (línea 7) — no exclusivo de esta fila. |
| `NXR-REL-001` | Un requisito solo sube a `IMPLEMENTADO Y VALIDADO` con código, integración, pruebas, permisos server-side, auditoría, errores, documentación, commit, SHA y CI. | CONFIRMADO | Regla explícita solicitada y reflejada en esta matriz. | Automatizar la verificación y registrar el resultado del SHA publicado. |
