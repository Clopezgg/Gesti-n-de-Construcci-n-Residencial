# Documento retirado

La arquitectura de Oracle Cloud ya no corresponde a la producción de ConstruControl.

No utilice instrucciones de Oracle, ARM64 ni Always Free para este repositorio.

La documentación vigente es:

```text
docs/deployment/AWS_COOLIFY.md
```

Producción actual:

- AWS EC2 x86_64;
- Ubuntu;
- Coolify;
- Docker Compose `/docker-compose.yml`;
- rama `main`.

Este archivo se conserva únicamente como redirección para enlaces históricos y no contiene procedimientos ejecutables.
